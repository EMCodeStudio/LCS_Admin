import { CollectionConfig } from "payload/types";

const PQR:  CollectionConfig = {
    slug: 'pqrs',
   labels: {
      singular: 'PQR',
      plural: 'PQRs',
    },
    admin: {
       // useAsTitle: '',
        group:'SISTEMA'
    },
    fields: [
        //example text field
       
       
       
       
       
    ],
   
   
   
    timestamps: true,
};

export default PQR;